size (250,250);
background (255);

float y = 50; 
line(0, y, width, y);
y = y * 0.5; 
line(0, y, width, y);
y = y * 0.5; 
line(0, y, width, y);
y = y * 0.5; 
line(0, y, width, y);
y = y * 0.5; 
y = y * 0.5; 
y = y * 0.5;
line(0, y, width, y);

